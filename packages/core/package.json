{
  "name": "@kilke/core",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf build && mkdir build && cp ./package.json ./build/package.json && cp -R ./src/* ./build"
  },
  "dependencies": {
    "any-observable": "^0.3.0",
    "axios": "^0.18.0",
    "reconnect-core": "^1.3.0",
    "winston": "^3.2.1"
  }
}
